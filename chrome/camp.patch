diff --git a/build/config/BUILD.gn b/build/config/BUILD.gn
index fceb8989236f3..7ffd220972990 100644
--- a/build/config/BUILD.gn
+++ b/build/config/BUILD.gn
@@ -68,7 +68,7 @@ config("feature_flags") {
     # setting use_ozone globally.
     defines += [ "USE_OZONE=1" ]
   }
-  if (is_asan || is_hwasan || is_lsan || is_tsan || is_msan) {
+  if (is_asan || is_hwasan || is_lsan || is_tsan || is_msan || is_camp) {
     defines += [ "MEMORY_TOOL_REPLACES_ALLOCATOR" ]
   }
   if (is_asan) {
diff --git a/build/config/compiler/BUILD.gn b/build/config/compiler/BUILD.gn
index 50be57ff8550e..58515925deb53 100644
--- a/build/config/compiler/BUILD.gn
+++ b/build/config/compiler/BUILD.gn
@@ -1627,7 +1627,7 @@ config("default_warnings") {
       if (!is_nacl) {
         cflags += [
           # TODO(crbug.com/1343975) Evaluate and possibly enable.
-          "-Wno-deprecated-builtins",
+          # "-Wno-deprecated-builtins",
 
           # TODO(crbug.com/1352183) Evaluate and possibly enable.
           "-Wno-bitfield-constant-conversion",
diff --git a/build/config/sanitizers/BUILD.gn b/build/config/sanitizers/BUILD.gn
index cb07c547122d8..4ee1f212a0bab 100644
--- a/build/config/sanitizers/BUILD.gn
+++ b/build/config/sanitizers/BUILD.gn
@@ -157,6 +157,12 @@ config("default_sanitizer_ldflags") {
 
   if (is_posix || is_fuchsia) {
     ldflags = []
+    if (is_camp) {
+      ldflags += [
+        "-L/home/moe/violet/build/src/safe_tcmalloc/tcmalloc",
+        "-ltcmalloc_tcmalloc", 
+      ]
+    }
     if (is_asan) {
       ldflags += [ "-fsanitize=address" ]
     }
@@ -382,6 +388,20 @@ config("hwasan_flags") {
   }
 }
 
+config("camp_flags") {
+  if (is_camp) {
+    cflags = [
+      "-O1", "-Xclang", "-load", "-Xclang",
+      "/home/moe/violet/build/src/compiler_pass/libProtectionPass.so",
+      "-L/home/moe/violet/build/src/safe_tcmalloc/tcmalloc",
+      "-ltcmalloc_tcmalloc", 
+      "-Qunused-arguments", 
+      "-Wpointer-arith", 
+      "-Wpointer-to-int-cast",
+    ]  
+  }
+}
+
 config("lsan_flags") {
   if (is_lsan) {
     cflags = [ "-fsanitize=leak" ]
@@ -514,6 +534,7 @@ all_sanitizer_configs = [
   ":common_sanitizer_flags",
   ":coverage_flags",
   ":default_sanitizer_ldflags",
+  ":camp_flags",
   ":asan_flags",
   ":cfi_flags",
   ":hwasan_flags",
diff --git a/build/config/sanitizers/sanitizers.gni b/build/config/sanitizers/sanitizers.gni
index ebc3c3e2365a1..9fc1927f9a4b3 100644
--- a/build/config/sanitizers/sanitizers.gni
+++ b/build/config/sanitizers/sanitizers.gni
@@ -10,6 +10,8 @@ import("//build/config/profiling/profiling.gni")
 import("//build/toolchain/toolchain.gni")
 
 declare_args() {
+  is_camp = false
+  
   # Compile for Address Sanitizer to find memory bugs.
   is_asan = false
 
